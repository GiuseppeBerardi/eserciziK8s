apiVersion: v1
kind: Pod
metadata:
  name: root-access-pod
  namespace: podroot
spec:
  containers:
    - name: ubuntu-container
      image: ubuntu:latest
      command: ["sleep", "infinity"]
      securityContext:
        runAsUser: 0
        runAsGroup: 0 
        privileged: true 
  restartPolicy: Never